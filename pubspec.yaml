name: flutter_application_2
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  device_info_plus: ^12.3.0  # Make sure this is aligned with 'flutter:'
  battery_plus: ^7.0.0
  installed_apps: ^1.0.1
  flutter_notification_listener_plus: ^1.3.7
  shared_preferences: ^2.5.4


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/LH8n.png
    - assets/wild_ksu_icon.png
    - assets/ksu_icon.png
    - assets/kernelsu_next.png
    - assets/apatch_icon.png
